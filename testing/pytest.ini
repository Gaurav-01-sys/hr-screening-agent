[pytest]
# Default base URL — override with --base-url CLI flag
# e.g. pytest testing/ --base-url https://your-app.streamlit.app
addopts = --base-url http://localhost:8501 -v --tb=short
testpaths = testing
